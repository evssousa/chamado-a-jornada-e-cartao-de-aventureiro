export default function CartaoDeAventureiro({ nome, classe, nivel }) {
    return (
        <div>
            <p>Nome: {nome}</p>
            <p>Classe: {classe}</p>
            <p>Nível: {nivel}</p>
        </div>
    )
}